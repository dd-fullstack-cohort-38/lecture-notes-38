// an array is a group of coma seperated values
// the symbol around an array's values is []

const students = ['Ron', 'Vian', 'Charles']
const grades = [.97, .98, .96]
console.log(students[1])
console.log(grades[1])

// for (starting point ; ending point ; count by)
for(let i = 0; i < 3; i = i + 1) {
  console.log (students[i]);
}